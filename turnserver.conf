# Coturn — Render / Docker (stdout logging via Docker CMD; long-term credentials).
listening-ip=0.0.0.0
listening-port=3478

realm=chatroom.app
lt-cred-mech
user=chatuser:supersecurepass123

fingerprint

no-cli
no-loopback-peers
no-multicast-peers

min-port=49152
max-port=65535
